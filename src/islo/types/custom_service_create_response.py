# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import UniversalBaseModel
from .auth_method import AuthMethod


class CustomServiceCreateResponse(UniversalBaseModel):
    """
    Newly created custom service descriptor.

    Mirrors the fields the modal needs to immediately kick off the connect
    flow (OAuth) or surface the API key form, without re-fetching.
    """

    app_id: str
    name: str
    slug: str
    auth_method: AuthMethod

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
