# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import UniversalBaseModel
from .auth_method import AuthMethod


class CustomService(UniversalBaseModel):
    """
    Tenant-wide custom integration metadata for the Add Integration picker.

    ``name`` is the free-form display label; ``slug`` is the normalized
    identifier embedded in ``descope_app_id`` and used as ``provider_key``
    in gateway rules.
    """

    descope_app_id: str
    name: str
    slug: str
    auth_method: AuthMethod

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
